package clubeleitura;

/**
 * Ponto de entrada do projeto. Serve só pra validar que o setup do
 * repositório e o compilador estão funcionando. Cada dev vai testando
 * o próprio bloco por aqui até a interface (front) estar pronta.
 */
public class Main {

    public static void main(String[] args) {
        System.out.println("Clube de Leitura Gamificado — setup ok!");
    }
}
